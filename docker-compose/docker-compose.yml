version: "3.8"

services:
  workprofile:
    build: ..
    ports:
      - "5000:5000"

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - workprofile
